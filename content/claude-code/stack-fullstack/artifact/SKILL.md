---
name: stack-fullstack
description: Full-stack dev stack for typed Next.js + Vercel/Netlify — installs the plugins and skills needed for the shipping loop (plan → build → review → deploy → monitor). Use when bootstrapping a new full-stack project.
---

# Full-stack dev stack

Installs 15 Claude plugins, 3 Codex plugins, and the `e2e-review` skill into the current project.

Run once after `frenxt stack stack-fullstack`. No state, no follow-up.
